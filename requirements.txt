# In your local project, update requirements.txt
echo "gunicorn" >> requirements.txt

# Or recreate the full file:
cat > requirements.txt << 'EOF'
Django>=4.2
djangorestframework
django-tailwind[reload]
django-cors-headers
psycopg2-binary
python-decouple
dj-database-url
requests
pillow
gunicorn
whitenoise[brotli]
EOF
